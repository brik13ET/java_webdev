services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=getting_started'
      - 'POSTGRES_PASSWORD=my_cool_pass'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
