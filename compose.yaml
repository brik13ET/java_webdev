services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=getting_started'
      - 'POSTGRES_PASSWORD=my_cool_pass'
      - 'POSTGRES_USER=my_cool_user'
    ports:
      - '5432'
